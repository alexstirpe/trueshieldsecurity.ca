{
  "name": "trueshieldsecurity-site-dev",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "stylelint": "^15.10.0",
    "stylelint-config-standard": "^32.0.0",
    "playwright": "^1.40.0",
    "@axe-core/playwright": "^4.6.0",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "lint:css": "stylelint \"css/*.css\" \"assets/css/*.css\" --fix",
    "serve": "npx http-server ./ -p 8080",
    "test:playwright": "npx playwright test",
    "test:responsive": "npm run serve & sleep 1 && npx playwright test && pkill -f http-server || true",
    "visual:test": "node tools/visual-test.js"
  }
}
